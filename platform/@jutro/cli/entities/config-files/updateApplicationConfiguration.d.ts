/**
 * Update config files in the new application based on the config provided
 * by the user.
 * @param {object} config - User gathered config info
 */
export function updateApplicationConfiguration(config: object): void;
