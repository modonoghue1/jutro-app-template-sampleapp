/**
 * Will determine which packages have been deprecated and will either prompt the user
 * for removal
 *
 * @param {object} options -
 * @param {string} options.packagePath - the path the the applications package.json file
 * @param {string} options.packagePrefix - the string prefix for the packages to be checked for deprecation
 * @param {boolean} options.isLogCaptured - flag for capturing the log
 */
export const clearDeprecated: any;
