/**
 * Decorator for mixing in a `translate(key, ...args)` into the
 * decorator class for retrieving displayed messages in the UI.
 * @returns {Function} - The decorator function
 */
export default function translator(): Function;
