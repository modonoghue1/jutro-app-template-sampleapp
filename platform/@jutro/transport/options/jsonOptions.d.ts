import { Options } from '../common';
/**
 * Provides JSON header for requests with JSON payload
 *
 * @example
 * HttpRequestBuilder().withOptions(jsonHeader).build()
 */
export declare const jsonOptions: Options;
