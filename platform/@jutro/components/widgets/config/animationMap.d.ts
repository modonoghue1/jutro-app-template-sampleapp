/**
 * Maps a named component to actual component class (and optional props)
 * Useful for json configuration definitions
 *
 * @param {string} name - The name of the animation to look up
 * @returns {object} - The properties for the named animation
 */
export function resolveAnimationFromName(name: string): object;
export const animationDelayDefault: 400;
