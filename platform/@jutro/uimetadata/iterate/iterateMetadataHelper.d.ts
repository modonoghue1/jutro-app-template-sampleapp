/**
 * Iterates over metadata
 * @param {object} args
 */
export function iterateMetadata({ contentAfter, contentBefore, contentEmpty, contentRepeat, contentRepeatAfter, contentRepeatBefore, repeatData, repeatPath, }: object): any;
