import stubTrue from"lodash/stubTrue";import get from"lodash/get";export const createTextFilter=filterValue=>{const words=filterValue.trim().toLowerCase().split(" ").filter(Boolean);return words.length?text=>{const lowerText=null==text?void 0:text.toString().toLowerCase();return words.some((word=>null==lowerText?void 0:lowerText.includes(word)))}:stubTrue};export const defaultColumnFilter=(filterValue,{path:path})=>{const textFilter=createTextFilter(filterValue);return item=>textFilter(get(item,path,""))};