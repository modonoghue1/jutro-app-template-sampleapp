/**
 * Toggle value in array
 *  - remove it it was there before
 *  - add it if it was not there before
 * @param {object} array
 * @param {object} value
 */
export function toggleValue(array: object, value: object): any;
