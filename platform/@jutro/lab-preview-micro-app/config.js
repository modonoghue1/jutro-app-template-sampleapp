import{getConfigValue}from"@jutro/config";export const getRemoteAddress=remoteScope=>{const configuredUrl=getConfigValue(`microAppConfig.remotes.${remoteScope}`,"");if(!configuredUrl)throw new Error(`Unable to find remote url configuration for ${remoteScope}. Make sure to set it in Jutro Config before calling importMicroApp.`);return(configValue=>{const urlMatch=configValue.match(/\w+:\/\/.*/);return null!==urlMatch?urlMatch[0]:configValue})(configuredUrl)};