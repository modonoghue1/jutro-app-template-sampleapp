export function Card({ maxWidth, children, title, id, headerAction }: {
    maxWidth: any;
    children: any;
    title: any;
    id: any;
    headerAction: any;
}): JSX.Element;
export namespace Card {
    const displayName: string;
    namespace defaultProps {
        const maxWidth: number;
    }
}
